# Todos

- Add per-user default resume theme preference and allow switching in profile
- Implement email deliverability check (MX + SMTP handshake) via a background validator service
- Configure Google OAuth client Id/secret in appsettings and Azure/hosting secrets
- Add account deletion page in Identity Manage area if not scaffolded by default UI
- Improve PDF layout spacing for multi-page resumes (page-break rules)
- Add unit/integration tests for Controllers and Identity flows
- Add rate limiting for auth endpoints and contact forms
- Add server-side validation summaries to all forms consistently
- Consider server-side PDF generation option (e.g., wkhtmltopdf or Playwright)
